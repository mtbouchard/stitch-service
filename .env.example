# Copy to .env for local overrides (optional - the app runs with defaults out of the box).
# APP_MODULE=solution_app   # which file Render serves: solution_app (reference) or app (yours)
# STITCH_DELAY=3            # seconds of fake compute so you can feel the awaited subprocess
# CORS_ALLOW_ORIGINS=*
