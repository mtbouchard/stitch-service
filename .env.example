# Copy to .env for local overrides (optional - the app runs with defaults out of the box).
# USE_REFERENCE=1          # run the complete reference instead of your stub
# STITCH_DELAY_SECONDS=3   # fake slow work so you can watch a job poll
# CORS_ALLOW_ORIGINS=*
# MAX_UPLOAD_BYTES=10485760
